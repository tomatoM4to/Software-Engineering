# KIS AI 트레이딩 어시스턴트 발표 대본 (기술 고도화 버전)

## 시간 구성

- 1~20페이지 설명: 약 8분 30초
- 최종 프로젝트 시연: 약 1분 20초
- 마무리 및 Q&A 전환: 약 10초
- 22~24페이지: 질문 대응용 부록, 본 발표 시간에서 제외

---

## 1페이지. 표지 (20초)

안녕하세요. 저희는 한국투자증권 API와 Claude AI를 결합한 실시간 매매 시그널 자동화 시스템, **KIS AI 트레이딩 어시스턴트**를 개발한 7조입니다.

저는 발표를 맡게된 입니다.

오늘 발표에서는 저희가 프로젝트 과정에서 마주한 기술적 한계점들을 짚어보고, 스트레스 테스트와 캐시 도입을 통해 시스템 성능을 어떻게 개선했는지 그 과정을 중점적으로 발표해 보겠습니다.

## 2페이지. 목차 (15초)

먼저 기존에 만들었던 시스템에 대한 remind를 한 후, 개선사항, 스트레스 테스트 전 후 결과를 보여드리고 최종 프로젝트 시연을 하겠습니다.

## 3페이지. 문제 인식과 목표 (30초)

개인 투자자는 짧은 시간 안에 차트와 뉴스를 함께 분석하기 어렵고, 감정적인 판단으로 손실을 반복하기도 합니다.

저희는 이를 해결하기 위해 이동평균선과 거래량을 이용하는 돌파 전략에 AI 분석을 결합했습니다.

보수적 에이전트와 공격적 에이전트가 각각 독립적으로 판단한 뒤 합의 과정을 거치기 때문에, 단일 모델의 편향성 리스크를 최소화했습니다.

## 4페이지. 전체 아키텍처 (35초)

시스템은 FastAPI를 중심으로 한 **Layered Architecture** 입니다.

Nginx가 HTTPS 통신을 종단하고 FastAPI 서버로 요청을 위임합니다.

API 계층은 AI 분석, 돌파 전략 스캔 등의 기능을 제공하며, 서비스 계층은 기술 지표 계산 로직과 에이전트 의사결정 오케스트레이션을 담당합니다.

외부 API로는 한국투자증권, Claude, 네이버 뉴스를 연동했으며, 전체 환경은 Docker Compose를 통해 컨테이너화하였고, GitHub Actions를 도입해 CI/CD 파이프라인을 자동화함으로써 개발과 배포의 안정성을 높였습니다.

## 5페이지. AI 매매 시그널 파이프라인 (40초)

AI 분석은 종목 코드 하나로 데이터 수집부터 결론 도출까지 **End-to-End 파이프라인**으로 수행됩니다.

먼저 KIS API를 통해 최대 240분의 분봉 데이터를 확보합니다. 이후 계산 모듈이 이평선 수렴도와 거래량 비율 등 기술 지표를 산출하고 최신 뉴스를 fetch 합니다.

조립된 데이터 컨텍스트를 기반으로 두 AI 에이전트가 비동기 병렬 분석을 수행합니다.

두 에이전트의 의견이 규합하여 최종적인 결과를 Response 합니다.

## 6페이지. 돌파 전략 알고리즘 (35초)
이번에도 다시 도메인 지식을 소개하겠습니다.

보통 주식이라 하면 새로로 생긴 캔들이라 불리는 차트를 많이 보시는데요, 해당 전략에선 캔들이 아니라 캔들의 평균을 이은 이동평균선이란것만 분석합니다.

첫번째로 Break 라는건 이동평균선들이 계속 모여져 있는 상태를 의미합니다. 프로젝트에선 사용자가 지정한 특정 선을 기준으로 하위 20개의 또다른 선들이 평균적으로 N% 이내료 모여져 있는 부분을 탐색합니다.

그냥 이동평균선을 이용하여 가격변화가 없는 차트를 찾는다 생각하면 됩니다.

왜 가격변화가 없는 차트를 찾냐면요, 가격변화가 없다는건 해당 주식회사의 가치가 변화가 없다는 뜻이란 가죠, 근데 갑자기 가격이 오르거나 거래량이 늘어난다는건, 해당 주식회사에 어떠한 일이 발생했다 라고 해석하는 겁니다.

그럼 이제 왜 뉴스를 수집하는지도 연결이 되는데요, 차트 자체는 가만히 있다가 갑자기 요동치는 차트를 찾는거고, AI 는 차트 분석 결과와 최신 뉴스를 분석하여 해당 point 가 노이즈인지 아니면 진짜 호재인지를 판단해 주는거죠

이러한 부분을 한국에선 스윙전략 이라고 부르고, 서구에선 breakout 이라고 보통 부른다고 합니다. 주식의 기술적 지표의 가장 기본적인 분석법 이기도 하구요

## 7페이지. KIS API 생산자 소비자 패턴 설명
한국투자증권에서 제공하는 API는 시스템 보호를 위해 '초당 20건'이라는 엄격한 호출 제한을 두고 있습니다. 많아 보일순 있지만

한번의 계산을 위해 최소 60건의 호출이 필요하기 때문에 엄격하게 KIS API 사용량을 제한해야 했습니다.

이전까지 저희는 이 문제를 해결하기 위해 비동기 기반의 Producer-Consumer 패턴을 사용했습니다.

사용자의 모든 요청은 즉시 API로 날아가는 것이 아니라, 먼저 asyncio.PriorityQueue(우선순위 큐)에 적재됩니다. 그러면 백그라운드에서 무한 루프로 동작하는 단일 컨슈머(Consumer)가 이 큐에서 요청을 하나씩 꺼내어 처리합니다.

이 컨슈머 내부에는 0.05초의 딜레이가 설정되어 있어, 동시 접속자가 아무리 폭주하더라도 시스템 스스로 KIS API의 '초당 20건 제한'을 절대 넘지 않도록 트래픽을 제어합니다.

해당 아키텍처의 문제점은 모든 요청을 단일 큐로 처리하다 보니까 뒤에있는 사용자는 Response 까지의 시간이 N배 걸린다는 점이죠.

## 8페이지. KIS API Throttling 및 캐시 전략 (35초)
그래서 저희같은 경우 캐시 레이어를 도입했습니다.

더이상 User 의 요청은 Queue 에 적재되지 않고 곧바로 캐시에 있는 데이터를 가져와 User 가 설정한 파라미터의 값을 기반으로 Pandas 연산을 통해 Breakout Point 를 찾아냅니다.

캐시 데이터 같은 경우는요, 백그라운드로 돌고 있는 스케줄러가 담당하게 됩니다. 해당 스케줄러가 Queue 를 사용하면서 초당 20건의 제한을 넘지 않도록 하면서 데이터를 계속해서 끊임없이 업데이트 하게 됩니다.

거래량이 높은 종목 30개를 기준으로 breakout 을 계산하기 때문에 해당 Ranking 을 5분마다 업데이트를 하고 해당 종목들의 차트 데이터 같은 경우 1분마다 업데이트 하도록 설계 했습니다.

코스피 30개, 코스닥 30개, 한종목당 최소 2번의 요청을 해야하기 때문에 총 120번의 데이터 fetch 가 1분마다 발생하게 됩니다.

마지막으로, 스케줄러의 Write 작업과 서비스 로직의 Read 작업이 동시에 일어날 때 발생할 수 있는 데이터 불일치 문제를 방지하기 위해, asyncio.Lock()을 적용하여 비동기 환경에서의 데이터 원자성과 안정성을 확보했습니다.


## 9페이지. OCI 배포 환경 (20초)

저희 서비스는 오라클 클라우드 인프라스트럭쳐에 배포되어 있습니다.

사용하고 있는 자원은 1core cpu, 1gb memory 를 가지고 있는 매우 열악한 하드웨어 환경에서 동작하고 있습니다.

이렇게 작은 컴퓨터가, 컨테이너화 되어 있는 Nginx, FastAPI 앱, 그리고 Docker engine, 그리고 여러 사용자의 Pandas 연산을 동시에 몇명까지 수행할 수 있는지 한번.. 보도록 해 봅시다.

## 9~12페이지. CI/CD 및 인프라 자동화 (1분 10초)
일단, 저희 팀 같은 경우 이미 중간 발표때 CICD 파이프라인을 구축했었기 때문에, 해당 파이프라인을 좀 자세히 설명 하도록 하겠습니다. 해당 구조로 배포만 거의 20번 넘게 진행하면서 뭐 특별히 문제가 생기지는 않았습니다.

### Step 1
저희는 Let's Encrypt의 Certbot을 1회용 Docker 컨테이너로 실행해서 인증서를 발급받는 방식을 썼구요. 발급된 인증서는 호스트의 `.nginx` 디렉토리에 마운트되어 Nginx 컨테이너와 공유됩니다.

해당 쉘 스크립트는 깃헙에서 clone 하거나 아님 vim 으로 복붙 하면 됩니다.

해당 스크립트를 실행하기 전에 80번 포트와 443 포트를 열어주어야 합니다.

### Step 2
다음은 Nginx 설정의 템플릿화 입니다.

저희는 도메인이 변경될 때마다 설정 파일을 일일이 하드코딩하는 비효율을 없애기 위해, Nginx의 Template 기능을 적극 활용했습니다.

해당 File 은 Nginx 컨테이너가 사용하고, 또 nginx 컨테이너는 GH Action 이 실행하기 때문에, Github 의 레파지토리의 환경변수를 업데이트 하면, 코드를 수정하지 않고 자동적으로 CI/CD 배포 라인을 유지할 수 있습니다.

내부적으로는 FastAPI 앱의 8000번 포트로 리버스 프록시 해주고요.

### Step 3
세 번째 단계는 Docker Compose를 활용한 멀티 컨테이너 아키텍처 구축입니다. 서비스는 백엔드, Nginx, Certbot 총 3개의 독립적인 컨테이너로 구성됩니다.

먼저 FastAPI 애플리케이션 이미지를 풀(Pull) 받아 구동한 뒤, 이어서 Nginx 컨테이너를 실행합니다. 이때 앞서 발급받은 SSL 인증서 디렉토리를 볼륨 마운트하여 리버스 프록시와 HTTPS 통신을 처리하게 됩니다.

마지막으로 Certbot 컨테이너는 백그라운드에서 12시간마다 인증서 갱신 여부를 체크합니다. 만약 인증서가 갱신되면 외부 개입 없이 **Nginx를 스스로 리로드**하도록 스크립트를 구성하여, 한 번의 배포로 만료 걱정 없는 무중단 자동화 환경을 완성했습니다.

### Step4
마지막 Step 4 는 GitHub Actions 파이프라인입니다.

크게 이미지 빌드와 배포, 헬스테스트 세 단계로 분리했습니다.

Main 브랜치에 코드가 푸시되면, 뭐 Main 엔 푸시를 못하니 Merge 되면이 맞지만요, 먼저 최신 이미지를 빌드해 Github Container Registry 에 Push합니다.

이후 Deploy 단계에서는 무거운 소스 코드를 서버로 보내는 대신, SCP Action으로 `docker-compose.yml` 과 Nginx 설정 디렉토리만 가볍게 전송합니다.

그리고 SSH Action으로 서버에 접속해 최신 이미지만 Pull 받아 컨테이너를 교체합니다.

## 13페이지. Health Check 검증 항목 (25초)
파이프라인의 마지막 단계에서 `health_check.py` 가 실행됩니다.

해당 스크립트는 총 4가지 end point 에 요청을해, Server 가 정상적으로 구동되어 있는지 테스트 합니다.

기본 서버 상태부터 AI 에이전트, 그리고 복잡한 연산이 필요한 **breakout api**와 뉴스 API까지 검사합니다.

캐시 구조 때문에, 캐시가 안정적으로 채워질때까지 60초의 대기시간을 가진다음 Health check 를 진행하구요, 일시적인 네트워크 오류를 방지하기 위해 **5번의 재시도(Retry)** 로직까지 넣어놨습니다.

만약 하나라도 끝까지 통과하지 못하면?  **Exit Code 1**을 뱉고 해당 Action 을 실패로 처리합니다. Action 이 처리되는거지 배포된 App 을 다시 배포를 수행하는 기능은 없습니다.

## 14페이지. 개선 전 성능 분석 (25초)

최초 시스템은 요청마다 외부 API를 동기적으로 호출하는 구조였습니다.

해당 그래프를 확인해 보면요, 파란색 그래프가 유저 수를 나타냅니다. 유저수가 3명일 경우, 보라색 그래프가 최대 40000ms 까지 치솟는 모습을 볼 수 있습니다. 이는 유저의 요청이 처리되는데까지 40초가 걸린다는 의미구요, 그 결과로 초록색 그래프, RPS 즉 초당 처리율이 0.14 로 사실성 서비스 불능 상태에 빠졌습니다.

여기서 만약 사용자가 1명이라도 추가가 되면 60초를 넘기며 타임아웃 에러가 발생하게 됩니다. 만약 유저들이 1분 정도는 기다린단 생각으로 서비스를 만들었다면 3명이 한계겠죠.

여기서 50th percentile 는 일반적인 사용자가 느끼는 평균적인 속도라 생각하면 되구요.

95th percentile 는 하필 트래픽이 몰릴때 접속한 운이 안좋은 5% 사용자가 겪는 속도라고 해석하면 됩니다.

## 15페이지. 개선 후 성능 지표 (30초)

캐시 레이어를 적용한 후, 동일 환경에서 **RPS가 15배 이상 향상된 2.2**를 기록했습니다.

동시 접속자를 20명으로 늘렸음에도 불구하고 응답 시간은 최악 12초, 평균 3초에서 6초 사이로 유지되어 있습니다.

흠 개인적으로, 성능이 미미하게 향상되었다고 생각했는데요, 처음엔 메모리가 1GB 니까 Disk IO 가 발생해서 느려진게 아닌가? 라고 생각했습니다. 근데 Server 의 대시보드를 확인해보니 Memory 가 부족하거나 DiskIO 그래프는 움직이지 않았습니다.

그래서 제 생각엔.. Docker 로 컨테이너화된 nginx 랑 fastAPI 앱을 실행하면서 pandas 연산까지 백그라운드로 실행하는건 1core cpu 로는 못돌리는건가..? 싶기도 하네요, 아직 의문이 끝나지는 않았습니다.

그래도 캐시를 적용해 성능이 10배 이상 향상된건 또 팩트니까 캐시가 아무일도 하지 않은건 확실히 아니긴 할텐데 말이죠

## 16~17페이지. 부하 테스트 자동화 및 요약 (45초)

지속적인 성능 모니터링을 위해 Locust 기반 부하 테스트를 GitHub Actions에 통합했습니다.

매번 로컬에서 명령어로 하는건 귀찮은 작업니까요.

개발자는 GUI를 통해 시나리오와 사용자 수를 설정할 수 있으며, 테스트 완료 후에는 스크립트가 지표를 자동 요약합니다.

부하 테스트 같은 경우 자동화 하기엔 실제 서비스에 영향을 주는 매우 무거운 테스트 방식이기 때문에 개발자가 수동으로 할수 있게 제작하였습니다.

깃헙 액션을 보시면 locust 를 headless 옵션으로 실행하는 모습을 볼수 있죠, 그리고 오른쪽 사진은 그 결과를 이미지와 요약 테이블 형태로 보여주는 log 입니다. 해당 테스트에선 10명으로 테스트 했구요, RPS 는 기존보다 조금더 향상된 2.4 를 기록했네요

## 18~19페이지. 코드 품질 자동화 (Ruff) (40초)

코드 스타일을 통합하고 안티패턴을 방지하기 위해 Ruff 를 도입했었는데요

이전엔 그저 PR 이 올라오면 Ruff 를 체크하고, 개발자에게에게 Ruff 체크를 하지 않았다고 경고성 메시지만 주는 방식이였는데요.

현실적으로 매번 로컬에서 Ruff 체크를 하는게 좀 많이 번거롭고 잘 하지 않게 되더라고요, 그래서 봇이 PR 이 올라오면 Ruff 체크를 하고 변경점을 자동으로 커밋하도록 변경하였습니다.

사진은 Bot 이 Ruff 체크를 자동화 해서 커밋을 하고 있는 사진입니다. auto fix and format with ruff 가 봇이 커밋한거구요

깃헙 액션 코드는 다음과 같습니다.

## 20페이지. 시스템 클래스 다이어그램 (35초)

클래스 다이어그램입니다. 보시다시피 컴포넌트 간의 책임 소재를 명확히 하기 위해 전형적인 계층형(Layered) 아키텍처를 채택했습니다.

최상단 Next.js 레이어에서 API 레이어로 HTTP 요청을 보내면, 이후 핵심 비즈니스 로직이 담긴 도메인 서비스 레이어로 단방향으로 흐르게 됩니다.

여기서 이번 구조의 가장 핵심적인 변화는 최하단 인프라스트럭처 레이어에 새롭게 도입된 KISCache 클래스입니다.

다이어그램을 보시면 도메인 서비스의 모든 데이터 요청은 외부 API로 직접 향하지 않고, 무조건 이 KISCache만을 바라보도록 설계되어 있습니다. 즉, 사용자의 요청이 한국투자증권(KIS)이나 외부망으로 직접 닿는 것을 아키텍처 단에서 원천 차단한 것입니다.

이상입니다. 감사합니다.

---

### Q1: KIS API 제한을 위해 PriorityQueue를 썼다고
했는데, 구체적으로 어떤 요청에 우선순위를 높게
주나요?
> A: "실시간으로 버튼을 누른 사용자의 분석
요청(priority=5)에 가장 높은 우선순위를 줍니다.
반면, 백그라운드에서 주기적으로 데이터를 갱신하는
스케줄러 작업(priority=10)은 상대적으로 낮은
우선순위를 부여합니다. 이렇게 하면 스케줄러가 수천
개의 데이터를 갱신하느라 큐를 점유하고 있더라도,
사용자가 분석 버튼을 누르는 순간 새치기를 해서 즉시
처리될 수 있도록 설계했습니다."

### Q2: Python의 requests는 동기(Blocking) 방식인데,
비동기 서버인 FastAPI에서 성능 저하가 없나요?
> A: "정확한 지적입니다. 그래서 저희는 모든 외부 API
호출을 asyncio.to_thread()로 감싸서 Thread Pool에서
실행하도록 구현했습니다. 덕분에 무거운 분석 작업이나
뉴스 수집 중에도 FastAPI의 메인 이벤트 루프는 멈추지
않고(Non-blocking) 다른 사용자의 요청을 동시에
처리할 수 있습니다."


### Q3: 왜 httpx 같은 비동기 라이브러리를 안 쓰고 requests를 asyncio.to_thread()로 감싸서 썼나요?
* 의도: 비동기 서버(FastAPI)에서 동기 라이브러리를 쓰는 것에 대한 성능 우려 확인.

기존 KIS API 연동 코드들과의 호환성과 안정성을 위해 requests를 유지하되, FastAPI의 이벤트 루프를 블로킹하지 않도록 asyncio.to_thread()를 사용해 별도 쓰레드 풀에서 실행했습니다. 이를 통해 비동기 서버의 장점인 동시성 처리를 그대로 유지하면서도 안정적인 통신이 가능하게 설계했습니다.

### Q4: asyncio.Lock()을 왜 썼나요? 동시 접속자가 많을 때 병목이 생기지 않나요?
   * 의도: 데이터 일관성(Race Condition) 해결 능력 확인.
백그라운드 스케줄러가 데이터를 쓰는(Write) 동안 사용자가 데이터를 읽는(Read) 작업이 동시에 일어날 때 데이터가 깨지는 것을 방지하기 위해 사용했습니다. 하지만 KISCache는 인메모리 딕셔너리 작업이라 연산 시간이 매우 짧기 때문에, 실제 사용자 체감 성능에는 거의 영향을 주지 않으면서 데이터의 원자성(Atomicity)을 보장할 수 있습니다.


### Q4: 아키텍처 다이어그램을 보면 Core 계층이 캡슐화되어 있다고 했는데, 무슨 뜻인가요?
* 의도: 객체지향 설계 원칙(Encapsulation) 적용 여부 확인.
서비스 레이어(AgentService, Scanner)는 내부적으로 큐가 어떻게  동작하는지, 0.05초 딜레이가 있는지 알 필요가 없습니다. 단순히 async_url_fetch를 호출하면 결과를 받는 구조입니다. 이렇게 인프라 로직을 Core 계층에 숨김으로써 비즈니스 로직은 순수하게 '분석 알고리즘'에만 집중할 수 있게 했습니다.

### Q5: deploy.yml 분석
* build-and-push 단계:
    * IMAGE_NAME: 이미지 이름을 소문자로 정규화하여 관리합니다.
    * docker/metadata-action: 이미지에 sha-{commit-hash}와 latest 태그를 동시에 부여합니다. 이는 버전 관리(SHA)와 최신본 유지(latest)를 동시에 하기 위함입니다.
    * GHCR (GitHub Container Registry): Docker Hub 대신 GitHub 자체
      저장소를 써서 보안과 속도를 높였습니다.
* deploy 단계 (가장 중요):
    * scp-action: 무거운 소스 전체가 아니라, 오직 docker-compose.yml과 .nginx 설정파일만 서버로 전송합니다. (리소스 최적화)
    * ssh-action: 서버에 직접 접속해 명령어를 내립니다.
        * sudo -E docker compose pull: -E 옵션은 현재 쉘의 환경변수(IMAGE_TAG, DOMAIN)를 sudo 명령으로 넘겨줍니다. 이 변수들이 docker-compose.yml 안의 ${IMAGE_TAG}에 주입됩니다.
        * sudo docker system prune -f: 1GB RAM 환경에서 남은 이미지 찌꺼기를 지워 디스크와 메모리를 확보하는 '필수 관리 로직'입니다.


### Q6: Docker Compose 분석 (docker-compose.yml)
이 코드는 "3개의 서비스가 유기적으로 연결된 아키텍처"임을 보여줍니다.

* backend 서비스:
    * expose: "8000": 8000번 포트를 외부에 열지 않고, Docker 내부 네트워크에서만 Nginx가 접근할 수 있게 격리했습니다. (보안 강화)
    * volumes: kis_devlp.yaml을 ro(Read-Only)로 마운트하여 설정 파일의 안전성을 확보했습니다.

* nginx 서비스 (Reverse Proxy & SSL Termination):
    * image: nginx:alpine: 가장 가벼운 Alpine Linux 기반 이미지를 써서 1GB RAM 환경에 최적화했습니다.
    * ./.nginx/conf.d:/etc/nginx/templates:ro: Nginx의 Template 기능을 씁니다. 배포 시 전달된 ${DOMAIN} 변수가 실제 설정 파일에 동적으로 주입됩니다.
    * volumes (certbot): 뒤에 설명할 certbot 컨테이너가 만든 인증서를 실시간으로 읽어와서 HTTPS(443)를 처리합니다.
* certbot 서비스 (SSL 자동화):
    * entrypoint: 12시간마다 무한 루프를 돌며 인증서 갱신을 체크합니다.
    * webroot 방식: Nginx가 떠 있는 상태에서 특정 경로(.well-known/acme-challenge)를 통해 소유권을 증명하고 인증서를 갱신하는 가장 깔끔한 방식입니다.


### Q7: 부하 테스트 분석 (locust-load-test.yml)
이 코드는 "서비스가 실전에서 얼마나 버티는가"를 정량적으로 측정하기 위한 도구입니다.

* 핵심 로직 (Workflow Dispatch):
    * on: workflow_dispatch: 이 테스트는 매번 자동으로 돌지 않습니다. 부하 테스트는 실제 서버에 스트레스를 주기 때문에, 개발자가 필요할 때만 트리거하도록 설계되었습니다. (안전 장치)
* Headless 실행:
    * locust -f tests/locust_breakout.py --headless -u 10 -r 2 --run-time 1m: GUI 없이 터미널 모드로 실행합니다. 10명의 유저가 초당 2명씩 늘어나며 1분간 공격하는 시나리오입니다.
* 결과 요약 (locust_summary.py):
    * 테스트가 끝나면 단순히 로그만 남기는 게 아니라, scripts/locust_summary.py를 실행해 RPS, 응답시간, 실패율을 깔끔한 테이블 형태로 GitHub Action 요약창에 출력합니다. (가독성 최적화)

### Q8: 코드 품질 관리 분석 (ruff.yml)
이 코드는 이른바 'Ruff Bot'의 심장부입니다. 팀원들이 서로 다른 스타일로 코딩하는 것을 기계적으로 차단합니다.

* 트리거 (pull_request):
    * PR이 올라오면 즉시 감시를 시작합니다.
* 자동 수정 (--fix):
    * ruff check . --fix: 단순히 에러를 찾기만 하는 게 아니라, 수정 가능한 부분은 알아서 고칩니다.
    * ruff format .: 코드 간격, 따옴표 스타일 등을 FastAPI 표준 스타일로 강제 통일합니다.
* 자동 커밋 (stefanzweifel/git-auto-commit-action):
    * 이 워크플로우의 핵심입니다. 린터가 코드를 수정했다면, 그 변경사항을 다시 해당 브랜치에 자동으로 커밋합니다.
    * 개발자는 "아, 린트 에러 났네" 하고 다시 수정해서 올릴 필요가 없습니다. 봇이 고치고 커밋까지 해주니까요.