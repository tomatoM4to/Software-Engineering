## 1. Page
안녕하세요, 소프트웨어공학 7팀입니다.
저희 팀은 기술적 지표에 AI Assistant 기능을 더해 매매 시그널을 자동화 하는 시스템을 만들었 습니다.
저는 발표를 맡게 된 김다원입니다.

## 2. Page
주요 링크 입니다. 발표가 끝나고 시연하겠습니다.

## 3. Page
목차는 다음과 같습니다.

## 4. Page
프로젝트 배경 및 목적입니다.

## 5. Page
개인 투자자의 가장 큰 적은 시간 부족과 감정적 판단입니다.
분봉 차트를 실시간으로 모니터링하고, 냉철하게 매매 결정을 내리는 것은 직장인이나 일반 투자자에게 현실적으로 매우 어렵습니다.
저희는 이 문제를 AI로 해결하고자 했습니다.

핵심 아이디어는 시스템이 정확한 연산을 통해 주요 지표들을 뽑아냅니다.

그리고 서로 성격이 다른 AI Agent 들이 뉴스와 함께 분석해 해당 종목을 구매해야하는지 아니면 지켜봐야하는지 지금팔아야 하는지 판단을 대신 해주는 아이디어 입니다.

해당 지표들이 수학적으론 만족하더라도, 주식 시장은 불확실성이 커서 반대로 이어지는 경우가 많기 때문에 이러한 아이디어를 생각 했구요.

## 6.Page
다음은 전체 시스템 아키텍처 입니다.

## 7.Page
시스템은 크게 세 개의 레이어로 구성됩니다.

클라이언트 요청은 Nginx를 통해 HTTPS로 수신되며, FastAPI 서버로 프록시됩니다.

FastAPI 서버는 네 가지 주요 엔드포인트를 제공합니다. AI 매매 시그널, 돌파 스캐너, 거래량 순위, 그리고 뉴스 검색입니다. 이밖에도 기타 엔드포인트가 있지만 생략하겠습니다.

서비스 레이어에서는 scanner, breakout, agent_data, agent_service, naver_news 모듈이 각 역할을 분담하여 처리합니다.

외부 API로는 KIS Open API, Anthropic Claude API, 네이버 뉴스 API 세 가지를 연동하고 있습니다.

전체 서버는 Docker 로 컨테이너화되어 있으며, GitHub Actions를 통해 Oracle Cloud Instance 에 자동 배포 됩니다.

## 8. Page
AI 매매 시그널 파이프라인을 소개하겠습니다.

## 9. Page
핵심 엔드포인트인 POST /api/agent/analyze/auto는 종목 코드 하나를 입력받으면 총 여섯 단계의 파이프라인이 자동으로 실행됩니다.

먼저 KIS API를 통해 총 240분 데이터를 수집합니다.

그 다음 Pandas를 이용해 MA5, MA20, MA60과 같은 이동평균선, 거래량 비율, 돌파 및 수렴 지표를 계산합니다. MA5 라 함은 5분 이동평균선을 의미합니다.

세 번째로 네이버 뉴스 API에서 해당 종목 관련 최신 뉴스 다섯 건을 수집합니다.

네 번째 단계로, 보수 에이전트와 공격 에이전트를 병렬 호출하여 동시에 분석을 수행합니다.

다섯 번째로 두 에이전트의 의견을 취합하여 합의 시에는 BUY 또는 SELL, 충돌 시에는 HOLD를 출력합니다.

마지막으로 포지션, 확신도, 목표가, 손절가, 분석 근거를 JSON 형태로 반환합니다.

## 10. Page
Breakout 전략이 정확히 무엇을 의미하는지 도메인 지식을 설명하겠습니다.

## 11. Page
보통 주식이라 하면 새로로 생긴 캔들이라 불리는 차트를 많이 보시는데요, 해당 전략에선 캔들이 아니라 캔들의 평균을 이은 이동평균선이란것만 분석합니다.

먼저

파란 실선이 앵커라 불리는 메인 선이구요, 그 외 점선들은 타겟이라 불리는 보조 선 입니다.

첫번째로 Break 라는건 해당 선들이 계속 모여져 있는 상태를 의미합니다. 프로젝트에선 앵커를 기준으로 하위 20개의 지표가 평균적으로 모여져 있는 부분을 탐색합니다.

왜 모여져 있는 부분을 찾냐면요, 모여져 있고 계속 유지가 되고 있단건 지금 시장에 참여하는 사람들, 개인 투자자든, 외국인이근, 증권사 기관이든 해당 주식회사는 이정도 가격이 적당하다고 암묵적인 합의를 본 상태라고 판단하는 겁니다.

근데 이러다가 갑자기 현재 가격이 앵커보다 위에 있으면서 동시에 거래량 까지 늘어났다? 지금 어떤 상황인진 모르겠지만 잔잔했던 물결이 깨지고 크게 오르거나 내리거나 할거란 신호라 해석하는거죠, 해당 부분을 Breakout Point 라고 부르고 해당 지표를 가진 회사들을 찾는것이 핵심입니다.

해당 방식은 Swing, 수렴 돌파 전략 등등 여러 이름으로 불리는 주식의 기술적 분석의 가장 기초적인 방법중 하나입니다.

## 12. Page
지표에도 여러가지가 있을수 있는데요, 저희 같은 경우 어떻게 정의했냐면요

None은 아무것도 미총족일떄

READY는 수렴일떄

NORMAL 은 수렴이면서 현재 가격이 앵커보다 위에 있을때, 그러면서 약간의 거래량 상승

BREAKOUT STRONG 은 Break이면서 Out 이면서 동시에 거래량까지 폭발적인 상승을 하는 상태를 의미합니다.

## 13. Page
AI 에이전트 의사결정 구조입니다.

## 14. Page
간단히 설명하면요, 보수적인 에이전트는 Strong 일때 뉴스와 함꼐 해당 종목을 사도 되는지 판단하는 Agent 고요

공격적 Agent 는 Normal 일때도 판단하는 Agent 입니다.

그리고 확신도 지표를 추가했습니다. Agent 가 판단을 했지만 해당 판단을 결국 마지막엔 사람이 판단해야 하기 때문에.. 지표를 판단하는 AI 를 판단하는 지표.. 인거죠

각 Agent 는 각각의 시그널을 Return 하는데요, 시그널 취합 규칙은 다음과 같습니다.
1. 둘다 BUY 면 Buy
2. 모두 SELL 이면 SELL
3. 의견이 갈리면 HOLD 입니다.


## 15. Page
다음은 저희 시스템의 핵심 장치인 KIS API 트래픽 제어 아키텍처입니다.

## 16. Page
한국투자증권에서 제공하는 API는 시스템 보호를 위해 '초당 20건'이라는 엄격한 호출 제한을 두고 있습니다. 많아 보일순 있지만

한번의 계산을 위해 최소 30건의 호출이 필요하기 때문에 2명 이상의 사용자가 동시에 분석 버튼을 누르게 되면 이 제한을 초과하여 키가 회수되게 됩니다.

저희는 이 문제를 해결하기 위해 비동기 기반의 Producer-Consumer 패턴을 도입했습니다.

사용자의 모든 요청은 즉시 API로 날아가는 것이 아니라, 먼저 asyncio.PriorityQueue(우선순위 큐)에 적재됩니다. 그러면 백그라운드에서 무한 루프로 동작하는 단일 컨슈머(Consumer)가 이 큐에서 요청을 하나씩 꺼내어 처리합니다.

이 컨슈머 내부에는 0.05초의 딜레이가 설정되어 있어, 동시 접속자가 아무리 폭주하더라도 시스템 스스로 KIS API의 '초당 20건 제한'을 절대 넘지 않도록 트래픽을 제어합니다.

여기서 일반 큐를 사용하지 않고 우선순위 큐를 사용한 점은, 실시간 사용자 요청이 백그라운드 스케줄러 작업 때문에 사용자의 요청이 처리되지 않는, OS 의 기아 문제와 비슷한 상황을 방지하기 위함이었습니다.

설계상 API 호출의 횟수를 줄이기 위해 내부적으로 캐시 DB 를 만들어서 주기적으로 스케줄링 작업을 해줬어야 했지만 아쉽게도 만들지는 못해 기아 문제가 진짜 해결되었는지는 확인하지 못했습니다.

하지만 여러명이 동시에 요청할때 트래픽을 제어하는 기능은 테스트 완료 했습니다. 단점이라면 모든 요청을 직렬화해서 단일 큐로 처리하기 때문에 뒤에 요청한 사람은 기간이 N배가 걸린다는 점입니다.

## 17. Page
다음은 저희 서비스의 인프라 아키텍처 및 CI/CD 배포 파이프라인입니다.

## 18. Page
먼저 배포 환경은 오라클 클라우드(Oracle Cloud)를 활용했습니다. 초기 단계에서는 80번과 443번 포트를 개방하고, 프로젝트를 clone 합니다. 이후 미리 작성한 쉘 스크립트를 통해 Certbot SSL 인증서를 발급 받습니다. 이렇게 발급된 인증서와 .env 같은 민감한 환경 설정 파일들은 배포 디렉토리에 격리하여 초기 세팅을 마칩니다.

초기 세팅 이후의 모든 배포 과정은 GitHub Actions를 통해 자동화되어 있어, 직접 서버에 접속해 명령어를 칠 일은 없습니다.

파이프라인은 main 에 코드를 병합(Merge)하는 순간 GitHub Actions 이 트리거됩니다.

CI(지속적 통합) 단계에서는 GitHub 환경 내에서 애플리케이션을 도커(Docker) 이미지로 빌드하고, 이를 Docker Hub 저장소에 푸시합니다.

이어지는 CD(지속적 배포) 단계에서는 GitHub Actions 이 오라클 클라우드 서버에 SSH로 원격 접속합니다. 그리고 Docker Hub에서 방금 빌드된 최신 이미지를 Pull 받은 뒤, 미리 세팅해둔 환경 변수들과 함께 docker-compose up 명령어를 실행하여 새로운 버전을 띄우게 됩니다.

FastAPI 같은 경우는 이렇게 배포 했구요, 이밖에 Frontend 는 Vercel 서비스를 이용해서 배포 했습니다.


## 19. Page
개발 과정입니다.

## 20. Page
이슈 목록 이구요, 저희팀 같은 경우 main 브랜치에 푸시하는걸 막는 Rule 를 적용했습니다. 그래서 머지를 하기 위해선 항상 PR 을 올려야 했구요, 추가로 해당 PR 에는 다른 팀원들이 리뷰 가 하나 이상 있어야 머지가 가능하도록 규칙을 추가 했습니다.

개발을 진행할땐, Ruff 를 적용해 소스코드 작성 스타일을 통일했습니다. Ruff 세팅은 FastAPI 의 공식 깃헙에서 사용된 Linter 세팅을 그대로 사용했구요.

추가로 PR 와 코멘트만으론 연락이 너무 불편해 디스코드를 메인으로 연락하면서 개발을 진행했습니다.

아 근데, 마감 기간이 여유로울땐 이렇게 했지만, 개발 후반기엔 다들 너무 바빠서 린터도 무시하고 Approve 규칙도 없앤체로 개발을 진행하긴 했습니다.

## 21. Page
API 응답 예시입니다.

## 22. Page
응답 예시는 스킵하고 다이어그램 구조 소개와 시연을 한다음 마무리 하겠습니다.

## 23. Page
아키텍처 구조 입니다.

이전에 소개했듯 Server 는 전체적으로 Router, Services, Core 계층으로 구성되어 있습니다.

그리고 백그라운드 스케줄러를 돌리는 Tasks 가 Core 계층에 연결되어 있습니다. 해당 스케줄러는 한국 투자증권 API 의 엑세스 토큰을 발급받는 스케줄러 입니다.

Server 를 처음 실행시키면 Consumer 를 백그라운드에서 무한루프로 돌리는 Worker 와 인증 스케줄러가 백그라운드로 실행되도록 설계했습니다.

이후에 소개해 시연하겠지만, 덕분에 무거운 연산을 하고 있는 중에도 Server 가 블로킹 되지 않습니다.

## 24. Page
클래스 다이어그램 입니다.

첫째로, 상단의 파란색 블록은 스키마(Schemas) 계층입니다. 시스템 내부 컴포넌트 간, 그리고 외부와의 통신에서 Request와 Response의 데이터 규격을 정의하는 역할을 수행합니다.

중앙의 초록색 블록은 핵심 비즈니스 로직을 담당하는 서비스(Services) 계층입니다. 라우터로부터 요청을 받아 돌파 매매 알고리즘 연산을 수행하고, 외부 뉴스 API 데이터 수집 및 LLM Agent LLM 호출을 총괄합니다.

마지막으로 하단의 보라색 블록은 시스템의 코어(Core) 계층입니다. 여기에는 한국투자증권 API의 액세스 토큰을 주기적으로 갱신하는 백그라운드 스케줄러, 앞서 소개했던 트래픽 제어용 Producer-Consumer Fetch 래퍼, 그리고 Agent를 구동하는 프롬프트가 위치해 있습니다.

화살표를 보시면, 스키마와 서비스 계층은 긴밀하게 연결되어 있지만 코어 계층은 레퍼와 프롬프트많이 외부 서비스와 연결되어 있고 나머지 구성 요소는 마치 커널처럼 캡슐화 하여 작동하도록 설계 했습니다.

## 시연
...

## 예상 질문
1. 트래픽 제어 아키텍처: 단일 병목점(Choke Point) 설계
User 의 요청시 Future 를 생성, Queue 에 put 함, 해당 Future 에 await 키워드를 걸어 백그라운드에서 해당 Future 가 완료될 때 까지 기다리는 구조, 해당 Queue 는 한국 투자증권 API 일 경우에만 사용됨

2. 블로킹 방지
requests 를 메인으로 사용했지만 뉴스를 제외한 모든 요청이 `asyncio.to_thread()` 로 감싸서 실행했기에 블로킹 되지 않음, news 도 블로킹 되지 않음, FastAPI 의 설계상 일반 `def` 으로 함수를 선언하면 자동으로 비동기로 동작한다고 알고 있음

3. 데이터 전처리 오케스트레이션 (LLM 할루시네이션 억제)
Ai Agent 에게 Request 를 할땐, 바로 차트부터 주입하는게 아니라, Pandas 를 이용해 breakout 포인트를 계산후 해당 수치와 뉴스를 묶어 JSON 형태로 Request 함, LLM 의 수학적 한계 극복하기 위한 설계

4. Agent 병렬 처리
`await asyncio.gather(...)` 로 동시에 실행, 이후 두 결과를 받아, `_aggregate_signals()` 최종 시그널 도출

5. 토큰 갱신의 Singleton 스케줄러 처리
tasks/auth_scheduler.py를 보면 AuthScheduler가 threading.Lock()을 이용한 Singleton 패턴으로
구현되어 있고, AsyncIOScheduler를 사용해 매일 밤 10시에 KIS 토큰을 백그라운드에서 갱신 (오후 10시)
